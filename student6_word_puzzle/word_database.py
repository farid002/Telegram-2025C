"""
Söz bazası - müxtəlif sözlər

TODO:
1. WORDS_DATABASE list yaradın - müxtəlif sözlər
2. get_words_by_length(min_length, max_length) - uzunluğa görə sözlər
3. get_random_word(min_length, max_length) - təsadüfi söz
4. is_valid_word(word) - sözün etibarlı olub olmadığını yoxla
"""

# TODO: WORDS_DATABASE = ["alma", "kitab", ...]


def get_words_by_length(min_length=None, max_length=None):
    """
    Uzunluğa görə sözləri qaytarır
    
    TODO:
    1. min_length və max_length filterləri
    2. Filter edilmiş list qaytar
    """
    # TODO: Uzunluq filterləmə
    return []


def get_random_word(min_length=None, max_length=None):
    """Təsadüfi söz qaytarır"""
    # TODO: random.choice() ilə söz seç
    return ""


def is_valid_word(word):
    """Sözün etibarlı olub olmadığını yoxlayır"""
    # TODO: WORDS_DATABASE-də varmı yoxla
    return False